// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCBWRnWFrNlrhqGvtoe0tF_MSlMk_lmxVE",
    authDomain: "thesis-24eab.firebaseapp.com",
    databaseURL: "https://thesis-24eab.firebaseio.com",
    projectId: "thesis-24eab",
    storageBucket: "thesis-24eab.appspot.com",
    messagingSenderId: "64663812224",
    appId: "1:64663812224:web:6fc78e20dfec7c655c8e65",
    measurementId: "G-T7BXN8CB9R"
}
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
